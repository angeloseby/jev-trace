[project]
name = "jev-client"
version = "0.1.0"
description = "Jev SystemOne client — TypeSafe SDK wrapper (Choice/Score/Noul)"
requires-python = ">=3.11"
dependencies = ["typesafe-sdk>=0.7.0", "pydantic>=2.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
