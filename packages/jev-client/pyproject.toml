[project]
name = "jev-client"
version = "0.1.0"
description = "Jev SystemOne client — Choice/Score/Noul"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "pydantic>=2.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
