[project]
name = "jev-trace-attribution"
version = "0.1.0"
description = "Attribution helpers — graph builders"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
