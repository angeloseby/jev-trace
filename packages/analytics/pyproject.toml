[project]
name = "jev-trace-analytics"
version = "0.1.0"
description = "Analytics helpers — failure distribution, trends"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
