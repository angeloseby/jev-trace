[project]
name = "jev-trace-schemas"
version = "0.1.0"
description = "Shared Pydantic schemas / enums for JevTrace"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
