[project]
name = "jev-trace-worker"
version = "0.1.0"
description = "Celery worker for JevTrace"
requires-python = ">=3.11"
dependencies = ["celery>=5.3", "redis>=5.0", "sqlalchemy>=2.0", "httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
